name: Deploy Static Website to EC2

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ 13.113.178.94 }}
          username: ${{ failover }}
          key: ${{ ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAINN+QAxaJsivv8zpouL11hdDWQmWj8xnOM71z2s6Pr/w yamac@DESKTOP-9EI6ST5 }}
          source: "./*"
          target: "/var/www/quonsuntory"
